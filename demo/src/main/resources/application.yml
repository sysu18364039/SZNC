spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/sznc?serverTimezone=Asia/Shanghai
    username: root
    password: Leng2018
    driver-class-name: com.mysql.cj.jdbc.Driver
#    以上是对mysql的连接的配置文件

mybatis:
  config-location: classpath:mybatis/config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml  #对应mapper映射xml文件所在路径
#  type-aliases-package: com.sznc.demo.model #对应实体类路径